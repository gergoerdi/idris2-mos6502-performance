module Main

import Kastely

import JS.Util
import JS.Buffer

-- %foreign "javascript:lambda:(s) => resolveFile(s)"
-- resolveFile : String -> IO ArrayBuffer

main : IO ()
main = do
  -- n <- initialize resolveFile
  -- consoleLog $ show n
  pure ()
